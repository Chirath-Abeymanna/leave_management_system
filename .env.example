# Application configuration
APP_ENV=development
APP_PORT=8080

# Database configuration
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=backend_db
DB_PORT=5432
DB_SSLMODE=disable

# JWT Settings
JWT_SECRET=supersecretkey
JWT_EXPIRATION=24h
